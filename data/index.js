const users = require("./users");
const resume = require("./resume");
const cv = require("./cv");
const coverLetter = require("./coverLetter");

module.exports = {
  users,
  resume,
  cv,
  coverLetter
};